{
  "name": "licheng-ai-site",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "dev": "vitepress dev site",
    "build": "npm run sync && vitepress build site",
    "verify": "node scripts/verify-site.mjs",
    "test": "npm run build && npm run verify && node --test tests/*.test.mjs",
    "preview": "vitepress preview site --host 127.0.0.1",
    "deploy": "npm run build && wrangler pages deploy site/.vitepress/dist --project-name=licheng-ai-site --branch=main",
    "dev:sync": "npm run sync && npm run dev"
  },
  "devDependencies": {
    "vitepress": "1.6.4",
    "wrangler": "4.129.0"
  },
  "overrides": {
    "vite": "6.4.3"
  },
  "dependencies": {
    "qrcode": "1.5.4"
  }
}
